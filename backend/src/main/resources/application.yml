spring:
  application:
    name: bandeutseolap

  config:
    import: "optional:file:.env[properties]"

  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect

  jackson:
    time-zone: Asia/Seoul

  data:
      redis:
        host: ${SPRING_DATA_REDIS_HOST}
        port: ${SPRING_DATA_REDIS_PORT}
        password: ${SPRING_DATA_REDIS_PASSWORD}

  kafka:
    bootstrap-servers : ${KAFKA_BOOTSTRAP_SERVERS}
    producer:
        key-serializer: org.apache.kafka.common.serialization.StringSerializer
        value-serializer: org.apache.kafka.common.serialization.StringSerializer
        acks: all                      # 메시지 유실 방지 (이력 데이터이므로 신뢰성 우선)
        retries: 3
        properties:
          enable.idempotence: true     # 중복 전송 방지
          linger.ms: 5                 # 약간의 배칭으로 처리량 개선
          spring.json.add.type.headers: false  # 컨슈머가 타입 의존 없게
    consumer:
        group-id: bandeutseolap
        auto-offset-reset: earliest
        key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
        value-deserializer: org.apache.kafka.common.serialization.StringDeserializer

logging:
  charset:
    console: UTF-8
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG
    com.cupid.qufit: DEBUG
    org.apache.kafka: INFO
    org.springframework.kafka: INFO
    com.dobidan.bandeutseolap: INFO

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  access-expiration: 3600000      # 1시간
  refresh-expiration: 1209600000  # 14일

file:
  upload-path: /uploads